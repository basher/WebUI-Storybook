import { Preview, Story } from '@storybook/addon-docs/blocks';

# Float
- This allows content to be "floated" so that text wraps around it. For example, an image.
- The default float direction is **LEFT**. This can be reversed.

<Preview>
    <Story id="ui-components-layouts-float--float" />
</Preview>
