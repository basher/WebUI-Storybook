<Meta title="Global Styles/Print styles" />

# Print styles

- Print styles can be tested via the browser's dev tools.
- It might also be worth disabling all UI components' JavaScript initialisation code by commenting out `uiInit();` in `.\src\ts\index.ts` in the [WebUI repo](https://github.com/basher/WebUI-Parcel).
