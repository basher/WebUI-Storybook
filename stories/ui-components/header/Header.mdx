import { Preview, Story } from '@storybook/addon-docs/blocks';

# Site header

## Visual representation of header structure
- Site header is comprised of semantic HTML5 landmark regions and other structural elements.
- Some CSS class names have been omitted for clarity.
- `<div class="kb-header">` is a `CSS Grid` container.
    - Please note that some inline styles (e.g. borders) have been applied to show the CSS Grid layout of the individual components inside the header.

<Preview>
    <Story id="ui-components-header--header" />
</Preview>

## Vertical header
- `<div class="kb-header kb-header--vertical">` is a `CSS Flex` container.

<Preview>
    <Story id="ui-components-header--vertical-header" />
</Preview>
