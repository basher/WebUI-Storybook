import { Preview, Story } from '@storybook/addon-docs/blocks';

# Ajax loader
- This is a hardcoded representation of the Ajax loader.
- In reality, the markup is programatically generated by JavaScript.
- We don't include the loader icon in the SVG sprite because the `<circle>` element would be in the shadow DOM, and therefore impossible to style.

<Preview>
    <Story id="ui-components-ajax-loader--ajax-loader" />
</Preview>
