import { Preview, Story } from '@storybook/addon-docs/blocks';

# Footer

## Accessibility considerations
- According to the [HTML5 spec](https://html.spec.whatwg.org/#the-nav-element), it it not necessary to wrap footer navigation in a `<nav>`, as the `<footer>` provides sufficient semantics.

<Preview>
    <Story id="ui-components-footer-footer--footer" />
</Preview>
